const initialState = {
  data: {},
  isLoading: false,
  error: null,
};

const userSliceReducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default userSliceReducer;
